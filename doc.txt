round hit is made by creating the levels procedurally upto 200 levels
by instantiating the rotating keys and randomly assigning the animations
the game an be expanded further by creating the custom prefabs for the keys and calling them in the move class level method
the bullets are spwning with in a certain limit of limit and contains independent script for destrot
and the enemy keys are also spawned randomly and assigned random animations according to the levels